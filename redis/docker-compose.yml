services:
  redis:
    image: redis:latest
    restart: unless-stopped
    ports:
      - 6379:6379 
    volumes:
      - ${REDIS_PERSISTENT_STORAGE}:/data
    networks:
     - homeserver
    container_name: redis
